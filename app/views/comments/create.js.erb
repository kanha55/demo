$("#comments").append("<%= escape_javascript(render partial: 'comment', locals: {comment: @comment}) %>");
document.getElementById("commenter_id").value = "";
document.getElementById("comment_id").value = "";